let fraction = {
    numerator: 2,
    denominator: 3
}
function additions(_fraction) {
    alert(_fraction.numerator + _fraction.denominator)
}
function subtraction(_fraction) {
    alert(_fraction.numerator - _fraction.denominator)
}
function multiplications(_fraction) {
    alert(_fraction.numerator * _fraction.denominator)
}
function degree(_fraction) {
    alert(_fraction.numerator / _fraction.denominator)
}
function reductions(_fraction) {
    while (fraction.numerator <= _fraction.denominator) {
        sum += numberOne;
        numberOne++;
    }
}